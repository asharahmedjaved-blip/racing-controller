# racing-controller
The code controls a racing car inside a track where it has 5 LIDAR as input. Utilizing the sensor data the two vehicle controls are determined, namely target velocity and steering angle. The code uses a simple PID controller to successfully complete the track and in least amount of time.
